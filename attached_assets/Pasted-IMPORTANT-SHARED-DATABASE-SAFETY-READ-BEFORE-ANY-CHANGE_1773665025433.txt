IMPORTANT — SHARED DATABASE SAFETY (READ BEFORE ANY CHANGE)

This project uses TWO separate applications connected to ONE shared production database:

1. Admin Saphala Self Prep
2. Saphala Self Prep Student Frontend

Both apps connect to the same Neon PostgreSQL database.

STRICT RULES (DO NOT VIOLATE):
1. NEVER run destructive schema operations.
2. NEVER use --accept-data-loss.
3. NEVER drop columns or tables automatically.
4. NEVER remove “unused” columns unless explicitly instructed.
5. Only ADDITIVE schema changes are allowed.
6. If a column exists in the database but is not referenced in code, PRESERVE it.
7. Always assume the other app may depend on that column.
8. Production database structure must remain stable.

SAFE PRACTICES:
- Prefer ALTER TABLE ADD COLUMN IF NOT EXISTS
- Never perform automatic cleanup of “orphan” columns
- Never modify schema without checking both apps
- Protect production stability above refactoring or aesthetics

-------------------------------------

APP CONTEXT
This prompt is for the Saphala Self Prep Student Frontend app.

TASK — Enforce unlock-date scheduling in student access logic

Goal:
Student-facing content should remain locked until its configured unlock date/time, even if uploaded/published earlier.

Assume admin-side models now support an optional field like:
- unlockAt DateTime?

This should apply to:
- Tests
- Videos
- Ebooks / HTML content
- Flashcards
- PDFs

Business rule:
A student can access content only if:
1. it is published
2. student has access rights (free / purchase / entitlement as applicable)
3. unlockAt is null OR unlockAt <= now

If unlockAt is in the future:
- content should appear locked/scheduled as appropriate
- student must not be able to access it early

Required work:

PART A — Audit content access paths
Inspect the student frontend/backend access logic for:
- TestHub listing / brief / start
- Course detail pages
- Ebooks / HTML content pages
- PDF access
- Flashcard access
- Video access (if already present in frontend)
- any shared product-card/listing helpers

PART B — Enforce unlockAt
Update queries, mappers, and server-side access checks so future-dated content is not accessible early.

Requirements:
- locked before unlock date
- accessible on/after unlock date if other rules allow
- direct URL access must also respect unlock date
- do not rely only on UI hiding

PART C — UI state
Where appropriate, show clear student-facing states such as:
- Locked until [date]
- Available on [date]
Keep it minimal and clear.

PART D — Deliverables
A. Exact files/routes/helpers changed
B. Exact access rule implemented
C. Which content types are already covered
D. Which content types are not yet wired and why
E. Confirmation that future-dated content cannot be accessed early