We need to fix the student test experience in Saphala Self Prep frontend.

Confirmed issues in the test/attempt/review flow:

1. Raw editor metadata is visible in question and option HTML
Example:
<p data-start="1042" data-end="1143">...</p>

2. Question palette numbering starts from 0 instead of 1

3. The actual explanation uploaded in admin is not being shown correctly.
Instead, the frontend is showing a generic fallback like:
“The correct answer is Option A”
This fallback should only appear when no real explanation exists in the DB.

4. Tests support a maximum of 2 languages.
Examples:
- English + Telugu
- English + Hindi
- English + Kannada
Frontend must render these cleanly without mixing raw HTML/editor artifacts.

Goal:
Fix rendering and explanation behavior in the student frontend without changing admin storage here.

Required work:

PART A — Clean HTML rendering
1. Locate where the following are rendered:
- question stem
- options
- explanation
- review/solution view content

2. Add sanitization before rendering.
Remove editor-only metadata such as:
- data-start
- data-end
- other unnecessary data-* attributes coming from the editor

3. Preserve legitimate tags:
- p
- strong / b
- em / i
- br
- ul / li
- sup / sub

4. Apply the sanitization consistently in:
- attempt page
- review page
- solution/explanation section
- any Telugu/secondary-language expanded view

PART B — Fix question palette numbering
1. The question palette display currently starts from 0.
2. Change display numbering to start from 1.
3. Keep internal indexing zero-based if needed.
4. Ensure palette navigation and selected state still work correctly.

PART C — Fix explanation rendering
1. Audit where explanation is derived on:
- test attempt page (if visible later)
- review page
- solution/result page

2. Ensure the frontend shows the actual explanation stored in the DB.

3. Only use a fallback like:
“The correct answer is Option X”
when:
- explanation is null
- explanation is empty
- explanation contains only blank/editor-noise content

4. If bilingual explanation exists, render:
- primary explanation
- secondary explanation
cleanly and separately

PART D — Bilingual test rendering (max 2 languages)
1. Tests support maximum 2 languages only.
2. Frontend should correctly render:
- primary language content
- optional secondary language content
3. Do not merge them incorrectly in the display layer.
4. If the data is already separated in DB, render it properly.
5. If only primary exists, render only primary cleanly.

PART E — Deliverables
A. Exact files changed
B. Exact sanitization logic added
C. Exact explanation rendering fix
D. Confirmation that actual uploaded explanation now shows
E. Confirmation that fallback explanation appears only when no real explanation exists
F. Confirmation that palette numbering now starts from 1
G. Confirmation that question/option/explanation HTML now renders without editor metadata

Important:
This prompt is for the Saphala Self Prep Frontend app only.
Do not redesign admin upload/storage here.