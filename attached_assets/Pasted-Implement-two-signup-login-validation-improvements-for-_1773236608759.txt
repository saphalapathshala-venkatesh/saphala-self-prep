Implement two signup/login validation improvements for Saphala Self Prep without breaking existing auth, sessions, admin routes, or student flows.

PART A — Password strength validation

Add password validation for account creation.

Rules:
- Password is required
- Minimum length: 8 characters
- Must contain at least 1 letter
- Must contain at least 1 number

Validation behavior:
- Show a clear friendly inline error if the password does not meet requirements
- Do not allow account creation until the password is valid

Recommended error message:
"Password must be at least 8 characters long and include at least one letter and one number."

Apply this validation on:
1. frontend signup form
2. backend/signup API validation as well

Do not weaken existing password hashing or auth logic.

-----------------------------------------------------

PART B — Mobile number validation (mandatory 10 digits)

Update the signup form so Mobile Number is mandatory and must be a valid 10-digit Indian mobile number.

Rules:
- Required field
- Must contain exactly 10 digits
- Digits only
- No letters
- No spaces inside stored value
- No special characters
- Reject anything shorter or longer than 10 digits

Normalization:
- Trim surrounding spaces
- If user enters spaces around the value, remove them
- Store normalized digits-only value

Examples:
Valid:
9876543210

Invalid:
98765
98765432101
98 76543210
+919876543210
abcdef1234

Recommended error message:
"Enter a valid 10-digit mobile number."

Apply this validation on:
1. frontend signup form
2. backend/signup API validation as well

-----------------------------------------------------

PART C — Duplicate prevention compatibility

Ensure the existing duplicate-mobile check continues to work using the normalized 10-digit mobile value.

If the mobile already exists, continue showing:
"An account with this mobile number already exists. Please log in instead."

-----------------------------------------------------

PART D — Existing users and safety

Important constraints:
- Do not break login for existing users
- Do not break admin accounts
- Do not break signup success / login success messages
- Do not break case-insensitive email handling already added
- Keep TypeScript clean
- Ensure Vercel compatibility

If existing users already have valid 10-digit mobile numbers, they should continue working normally.

If any legacy records have invalid mobile formats, do not break current login immediately; only enforce strict 10-digit validation for new signups and new profile saves in this phase.

-----------------------------------------------------

After implementation provide:
1. files changed
2. validation rules added
3. how mobile normalization works
4. how legacy users are handled
5. exact test steps for:
   - weak password rejection
   - valid password acceptance
   - invalid mobile rejection
   - valid 10-digit mobile acceptance
   - duplicate mobile rejection