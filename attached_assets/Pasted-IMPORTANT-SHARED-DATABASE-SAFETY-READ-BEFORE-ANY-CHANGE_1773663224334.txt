IMPORTANT — SHARED DATABASE SAFETY (READ BEFORE ANY CHANGE)

This project uses TWO separate applications connected to ONE shared production database:

1. Admin Saphala Self Prep
2. Saphala Self Prep Student Frontend

Both apps connect to the same Neon PostgreSQL database.

STRICT RULES (DO NOT VIOLATE):

1. NEVER run destructive schema operations.
2. NEVER use --accept-data-loss.
3. NEVER drop columns or tables automatically.
4. NEVER remove “unused” columns unless explicitly instructed.
5. Only ADDITIVE schema changes are allowed.
6. If a column exists in the database but is not referenced in code, PRESERVE it.
7. Always assume the other app may depend on that column.
8. Production database structure must remain stable.

SAFE PRACTICES:

• Prefer ALTER TABLE ADD COLUMN IF NOT EXISTS
• Never perform automatic cleanup of “orphan” columns
• Never modify schema without checking both apps
• Protect production stability above refactoring or aesthetics

We are close to launch, so schema stability is critical.

-------------------------------------

APP CONTEXT
This prompt is for the Saphala Self Prep Student Frontend app.

TASK — FRONTEND PRODUCTION CRASH STABILIZATION

Problem:
The Self Prep frontend app is still crashing in production with application/server-side errors. Login sometimes fails, TestHub sometimes crashes, and the frontend is not stable enough for launch.

Goal:
Audit and stabilize the student frontend app so the critical student journey does not crash in production.

CRITICAL PATH TO AUDIT
1. login
2. session lookup / current user
3. dashboard
4. /testhub listing
5. test brief page
6. start attempt API
7. attempt page
8. submit/review/result page

REQUIRED WORK

PART A — Crash source audit
1. Inspect all server components, route handlers, and DB helpers used by:
   - /login
   - /dashboard
   - /testhub
   - /testhub/tests/[id]/brief
   - /api/testhub/attempts/start
   - attempt/review/result flow

2. Find exact crash causes, especially:
   - Prisma fields selected in code but missing in DB
   - queries using stale schema assumptions
   - null/undefined access in server components
   - session/user lookup failures
   - access logic mismatches
   - production-only env/runtime differences

3. Identify whether the current crash is:
   - DB/schema mismatch
   - frontend server-component bug
   - auth/session bug
   - access-rule bug
   - stale deployment/build issue

PART B — Stabilize launch-critical flows
Make the smallest safe fixes so these flows do not crash:
- existing user login
- dashboard load
- TestHub listing
- free test access
- locked test handling
- attempt start
- review/result load

Important:
If some optional feature data is missing, return safe fallback UI/state instead of crashing the whole page.

PART C — DB/runtime safety
1. Audit every Prisma select/include used in launch-critical frontend code.
2. If code references DB fields that may be missing, either:
   - restore them safely using additive DB changes only
   - or make the query defensive if the feature is optional
3. Do not drop or clean up any columns.

PART D — Logging and diagnostics
Add safe server-side logs for launch-critical failures so Vercel errors can be diagnosed quickly.
Do not leak internals to users.

PART E — Deliverables
A. Exact root cause(s) of the current frontend crashes
B. Exact files changed
C. Exact DB fields/tables that were missing or mismatched, if any
D. Confirmation of which critical flows now work
E. Any launch blockers still remaining
F. Final statement: “Student frontend critical path is stabilized for launch” only if truly verified

Important:
This is a launch stabilization task, not a feature task.
Prefer preservation, defensive coding, and runtime stability over cleanup or refactoring.